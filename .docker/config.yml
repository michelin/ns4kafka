kafkactl:
  contexts:
  - context:
      api: http://localhost:8080
      namespace: myNamespace
      user-token: myGitLabToken
    name: local
  current-namespace: myNamespace
  api: http://localhost:8080
  user-token: myGitLabToken
